// DEFAULT_MODEL is Haiku deliberately: it's the cheapest of the three and
// good enough for most explanations, so it stays the default everywhere a
// model isn't picked explicitly. Sonnet and Opus are opt-in, for harder code.
export const DEFAULT_MODEL = "claude-haiku-4-5";
export const SONNET = "claude-sonnet-5";
export const OPUS = "claude-opus-5";
