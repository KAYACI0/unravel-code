---
"unravel-code": patch
---

Fix the manifest's `publisher` field to match the actual Marketplace
publisher account (`AhmetKAYACI`). The placeholder value (`unravel-code`)
made every Marketplace upload fail with a publisher ID mismatch.
