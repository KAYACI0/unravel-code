---
"unravel-code": minor
---

Default `unravelCode.auth` to `claudeCode`, so a fresh install runs on the
user's Claude subscription with no API key step at all. Set it to `apiKey` to
use an Anthropic key instead.
